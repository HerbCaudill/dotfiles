interface:
  display_name: "Read agent sessions"
  short_description: "Find and read local Claude and Codex sessions"
  default_prompt: "Use $read-agent-sessions to find and summarize the relevant recent agent session."
