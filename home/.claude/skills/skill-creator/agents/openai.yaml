interface:
  display_name: "Skill creator"
  short_description: "Create or update a managed personal skill"
  default_prompt: "Use $skill-creator to create or update a personal skill in the managed dotfiles source."
policy:
  allow_implicit_invocation: false
