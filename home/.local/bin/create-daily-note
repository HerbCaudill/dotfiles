#!/bin/bash
# Create daily note stubs for any missing days from the last 30 days through tomorrow.
DAILY_DIR="/Users/herbcaudill/Code/HerbCaudill/notes/daily"

for i in $(seq -1 30); do
  d=$(date -v-${i}d +%Y-%m-%d)
  f="$DAILY_DIR/$d.md"
  if [ ! -f "$f" ]; then
    touch "$f"
  fi
done
